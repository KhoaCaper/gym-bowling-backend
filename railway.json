{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "healthcheckPath": "/",
    "healthcheckTimeout": 600,
    "restartPolicyType": "on_failure",
    "startCommand": "sleep 30 && java -jar app.jar"
  }
}
