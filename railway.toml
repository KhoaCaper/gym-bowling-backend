[build]
builder = "GRADLE"

[deploy]
startCommand = "java -jar build/libs/gym-bowling-backend.jar"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[deploy.environment]
JAVA_VERSION = "17"
